# claude-code-manager の環境変数サンプル。`.env` にコピーして値を埋める。
# `.env` は .gitignore 済み。`ai-monitor` 起動時に dotenv で読まれる。

# AI Monitor のダッシュボード AI 要約に使う Claude API キー。
# 未設定の場合、要約機能は黙って無効化され、カードには「(要約: API キー未設定)」と表示される。
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
